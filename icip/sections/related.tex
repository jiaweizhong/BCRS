\section{Related Work}
\label{sec:related}

\textbf{Small Object Detection in Aerial Imagery.} Detecting small and tiny targets in aerial and maritime imagery is a longstanding challenge due to extreme scale variations, low contrast, and complex background textures \cite{VisDrone,UAVDT,TinyPerson,SeaDronesSee}. Early approaches often utilize image-level cropping or density-guided patch generation (e.g., ClusDet \cite{ClusDet}, DMNet \cite{DMNet}, UFPMP-Det \cite{UFPMP}). While these methods reduce the area processed at high resolution, they typically rely on separate coarse detector stages or incur repeated feature extraction on overlapping crops. Other works focus on specialized label assignment strategies and geometric loss functions for micro-scale bounding boxes, such as Normalized Gaussian Wasserstein Distance (NWD) \cite{NWD}, RFLA \cite{RFLA}, and scale-aware balancing networks (SSABNet) \cite{SSABNet}. However, these dense detection frameworks still execute full-resolution convolutions uniformly across entire images, failing to eliminate massive spatial computation on empty background areas.

\textbf{Selective and Dynamic Computation.} To mitigate arithmetic redundancy, selective computation dynamically routes computations to spatial regions containing prospective targets. AutoFocus \cite{AutoFocus} predicts focus pixels on coarse scales to guide fine-scale processing. QueryDet \cite{QueryDet} accelerates high-resolution feature pyramids via cascaded sparse queries, while CEASC \cite{CEASC} formulates adaptive activation masking (AMM) to skip background locations in sparse convolutions. ESOD \cite{ESOD} advances this paradigm by integrating an early ObjSeeker module right after the shallow network stem, slicing features into patches (AdaSlicer) and bypassing background regions across all subsequent backbone, neck, and detection head stages. Nonetheless, existing selectors share two critical limitations: (i) they rely exclusively on single semantic objectness scores, which easily collapse for tiny objects under downsampling; and (ii) they are supervised via per-cell classification losses that offer no object-level coverage guarantees for micro instances.

\begin{table*}[t]
\centering
\caption{Systematic comparison of related detection paradigms and the proposed DES-ESOD framework.}
\label{tab:related}
\small
\resizebox{\textwidth}{!}{%
\begin{tabular}{lcccc}
\toprule
\textbf{Method} & \textbf{Routing Granularity} & \textbf{Evidence Source} & \textbf{Coverage Guarantee} & \textbf{Decoupled Head Design} \\
\midrule
QueryDet \cite{QueryDet} & Feature Pyramid / Query & Semantic only & \texttimes & Standard Conv Head \\
CEASC \cite{CEASC}       & Activation Masking      & Semantic only & \texttimes & Standard Conv Head \\
ESOD \cite{ESOD}         & Early Spatial Patch     & Semantic only & \texttimes & Standard / Sparse Head \\
SET \cite{SET}           & Full Dense Feature Map  & Spectral Filtering & \texttimes & Standard Conv Head \\
SSABNet \cite{SSABNet}   & Full Dense Feature Map  & Semantic only & \texttimes & Dense Balancing Head \\
\midrule
\textbf{DES-ESOD (Ours)} & \textbf{Early Spatial Patch} & \textbf{Dual (Semantic + Spectral)} & \textbf{\checkmark} & \textbf{Lightweight ISPP + SABL} \\
\bottomrule
\end{tabular}%
}
\end{table*}

\textbf{Spectral Analysis and Spatial Saliency.} Frequency-domain methods provide orthogonal insights into visual representations. SET \cite{SET} demonstrated that tiny objects preserve distinct high-frequency structural signatures even when their semantic features are weak, using frequency background smoothing to enhance dense feature representations. In contrast to SET, which applies dense spectral operations over entire multi-scale feature maps, DES-ESOD repurposes spectral and gradient saliency as an ultra-lightweight, channel-pooled routing signal ($\mathcal{O}(1)$ relative to backbone width), directly resolving the false-negative vulnerability in spatial selective computation.
